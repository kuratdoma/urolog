# Test bağımlılıkları.
#
# Üretim imajı bunları kurmaz (requirements.txt'e eklenmedi); CI ve lokal test
# çalıştırmaları bu dosyayı kullanır:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
